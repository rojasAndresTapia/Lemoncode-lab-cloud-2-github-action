import React from 'react';
import { hot } from 'react-hot-loader/root';
import { Main } from './pods/components/Main/Main';

const App: React.FunctionComponent = () => {
  return <Main></Main>;
};

export default hot(App);
